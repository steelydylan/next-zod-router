const path = require("path");

export default {
  moduleNameSpace: ".next-zod-router",
  baseDir: path.resolve("."),
  pagesDir: path.resolve("pages"),
  distDir: path.resolve("node_modules/.next-zod-router"),
};
